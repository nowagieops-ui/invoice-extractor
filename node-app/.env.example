# Local dev only — in production these are set via Hostinger's Node.js
# Selector "Environment Variables" UI, not this file.

PORT=3000
GEMINI_API_KEY=
# Any random string — required on all /debug/* routes so a public URL
# can't be used by strangers to burn Gemini quota or probe the disk.
DEBUG_TOKEN=
